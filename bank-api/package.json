{
  "name": "@midnight-bank/bank-api",
  "version": "0.1.0",
  "description": "Midnight Bank - API Layer for Privacy-Preserving Banking",
  "author": "Midnight Developer",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "clean": "rm -rf dist/",
    "typecheck": "tsc --noEmit", 
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "prepack": "npm run build"
  },
  "dependencies": {
    "@midnight-bank/bank-contract": "file:../bank-contract",
    "@midnight-ntwrk/compact-runtime": "^0.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.2.2",
    "vitest": "^1.0.0",
    "rxjs": "^7.8.2"
  },
  "files": [
    "dist/"
  ]
}